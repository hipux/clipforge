# Whisper model size: tiny (~75MB), base (~150MB), small (~500MB), medium (~1.5GB)
# Larger models are more accurate but slower
WHISPER_MODEL=base

# Backend host and port (default: localhost:8000)
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# Frontend port (default: 5173)
FRONTEND_PORT=5173
