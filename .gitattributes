# Windows batch files - store with CRLF, checkout with CRLF
*.bat text working-tree-encoding=UTF-8 eol=crlf
*.cmd text working-tree-encoding=UTF-8 eol=crlf

# Shell scripts - always LF
*.sh text eol=lf

# Default
* text=auto
